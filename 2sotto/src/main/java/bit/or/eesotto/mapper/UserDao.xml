<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.UserDao">

	<select id="getUser" resultType="bit.or.eesotto.dto.User">
		select userid, nick, pwd, loc, cpnumber, enabled, profile, rtime, lat, lot, point
		from user
		where userid=#{uid}	
	</select>
	
	<insert id="insert" parameterType="bit.or.eesotto.dto.User">
		INSERT INTO USER(USERID, NICK, PWD, LOC, CPNUMBER, ENABLED, RTIME)
		VALUES(#{userid}, #{pwd}, #{nick}, #{loc}, #{cpnumber}, 1, SYSDATE)
	</insert>
	
	<update id="updateMember">
		update member
		set pwd = #{pwd}, name=#{name}, cphone=#{cphone}, email=#{email}
		where userid = #{userid}
	</update>
	
	<!-- 가입시 id 중복 체크 -->
	<select id="idCheck" resultType="int">
	SELECT COUNT(*) FROM USER
	WHERE USERID = #{userId}
	</select>

</mapper>