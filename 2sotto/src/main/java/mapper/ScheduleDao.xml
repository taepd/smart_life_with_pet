<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bit.or.eesotto.dao.ScheduleDao">

	<insert id="newSchedule" useGeneratedKeys="true" keyProperty="sindex" parameterType="bit.or.eesotto.dto.Schedule">
		insert into SCHEDULE (petindex, userid, title, content, is_complete, start, end, allDay, daysofweek, adncdnoti, color, groupId) 
			values(#{petindex}, #{userid}, #{title}, #{content}, #{is_complete}, #{start}, #{end}, 
			#{allDay}, #{daysofweek}, #{adncdnoti}, #{color}, #{groupId})
	</insert>

</mapper>














