<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans.xsd
						http://www.springframework.org/schema/security 
						http://www.springframework.org/schema/security/spring-security.xsd">

	<security:http>
		<security:csrf disabled="true" />
		<security:form-login  default-target-url="/login/index.bit" login-page="/login/loginform.bit" authentication-failure-url="/login/index.bit?error" />
		<security:logout logout-success-url="/index.bit" />
	<!-- 	
	<security:intercept-url pattern="/customer/noticeDetail.do" access="hasRole('ROLE_USER')" />
	<security:intercept-url pattern="/customer/noticeReg.do" access="hasRole('ROLE_ADMIN')" /> 
	-->
		<security:intercept-url pattern="/customer/*Reg.do" access="hasRole('ROLE_USER')" />
		<security:intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
	</security:http>
	
	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service 
			authorities-by-username-query="select m.userid, r.role_name from member m join roll r on m.userid=r.userid where m.userid=?"
			users-by-username-query="select userid, pwd as password, 1 enabled from member where userid=?"
			data-source-ref="driverManagerDataSource" />
		
			<security:password-encoder ref="bCryptPasswordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>

</beans>